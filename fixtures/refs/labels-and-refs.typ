= Тест ссылок

Это документ для тестирования ссылок <sec:ref>.

== Секция с меткой

Содержимое секции <sec:content>.

Ссылка на заголовок: @sec:ref
Ссылка на секцию: @sec:content

Формула с меткой:

$$
E = mc^2
$$ <eq:energy>

Ссылка на формулу @eq:energy.

Таблица с меткой:

#table(
  columns: 1,
  [Данные],
) <table:data>

Ссылка на таблицу @table:data.
