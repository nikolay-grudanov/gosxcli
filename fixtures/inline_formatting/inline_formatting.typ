= Inline Formatting Examples

Here is *bold text* in a paragraph.

Here is _italic text_ in a paragraph.

Here is `inline code` in a paragraph.

This has *bold* and _italic_ and `code` all together.

As shown in @fig:example, the results are clear.

#figure(
  image("example.png"),
  caption: [Example figure for inline formatting test],
) <fig:example>