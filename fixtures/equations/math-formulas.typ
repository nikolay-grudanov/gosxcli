= Тест формул

Простая формула:

$$
E = m c^2
$$

Квадратное уравнение:

$$
x = (-b +- sqrt(b^2 - 4 a c)) / (2 a)
$$

Сумма:

$$
sum_(i=1)^n i
$$

Интеграл:

$$
integral_0^infinity e^(-x) d x
$$

Греческие буквы:

$$
alpha + beta = gamma
$$

Комплексная формула:

$$
integral_(-infinity)^infinity e^(-x^2) d x = sqrt(pi)
$$

Смешанная формула:

$$
f(x) = a_0 + sum_(i=1)^infinity (a_i cos(i x) + b_i sin(i x))
$$

Логарифм:

$$
log_e (a) = ln a
$$

Степени:

$$
2^10 = 1024
$$
